"""BLy dispersion and net-change waterfalls across the electricity PR chain.

Scripts:
- ``run_all`` — import cache (optional) and write waterfall PNGs
- ``import_local`` / ``refresh_cache`` — seed parquet cache from Excel or Sheets
- ``bly``, ``dispersion``, ``net_change``, ``waterfall`` — library helpers
"""
