"""Live model traces across v0.2 → reallocation → 3-way → mixed units.

Scripts:
- ``full_trace`` — markdown report of IO / E / D / N / BLy by config step
- ``decompose_d_n_step`` — detailed D/N walkthrough (appended to full trace)
"""
