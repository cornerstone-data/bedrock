"""Emission-factor (N/D) comparisons vs the Cornerstone v0.3 footing.

Scripts:
- ``plot_ef`` — per-step suite + 3-panel N/D histograms
- ``vs_footing_frames`` — frame builders for those plots
- ``analyze_n_variance`` — decomposes why sector N rises at 3-way / mixed units
"""
